{
  "name": "rave-raid",
  "version": "0.1.0",
  "description": "GOOPLIATH: DANCE RAID — a WebXR passthrough rhythm battle royale. Up to 24 dancers ring one giant dancing gel boss; every move he throws marks every platform at once. Dodge on the beat, build combos, outlast the floor. Built on the Immersive Web SDK.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "server": "node server/index.mjs",
    "analyze": "node tools/analyze-track.mjs"
  },
  "dependencies": {
    "@iwsdk/core": "0.4.2",
    "three": "0.184.0"
  },
  "devDependencies": {
    "@iwsdk/vite-plugin-dev": "0.4.2",
    "@types/three": "0.184.0",
    "playwright": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "ws": "^8.18.0"
  }
}
